import { Dashboard } from "@/views";

export const metadata = {
	title: "Dashboard — Traytic",
	description: "Your analytics dashboard.",
};

export default function DashboardPage() {
	return <Dashboard />;
}
