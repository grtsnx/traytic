import { TeamSettings } from "@/views";

export const metadata = {
	title: "Team Settings — Traytic",
	description: "Manage your team and organization.",
};

export default function TeamSettingsPage() {
	return <TeamSettings />;
}
