import { Onboarding } from "@/views";

export const metadata = {
	title: "Get started — Traytic",
	description: "Create your account and add your first site in under 5 minutes.",
};

export default function OnboardingPage() {
	return <Onboarding />;
}
