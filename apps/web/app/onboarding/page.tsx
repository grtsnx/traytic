import { Onboarding } from "@/views";

export const metadata = {
	title: "Get started — Traytic",
	description: "Create your account, add your site, and start capturing analytics in under 5 minutes.",
};

export default function OnboardingPage() {
	return <Onboarding />;
}
