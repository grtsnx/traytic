# ─────────────────────────────────────────────────────────────────────────────
# Traytic — Environment Variables
# Copy this file to .env and fill in the required values.
#
# For Docker (local or Coolify):
#   cp .env.example .env
#   # Edit BETTER_AUTH_SECRET (required)
#   # Edit NEXT_PUBLIC_API_URL if deploying to a real domain
#   docker compose up --build
# ─────────────────────────────────────────────────────────────────────────────


# ── Databases ─────────────────────────────────────────────────────────────────
# These are used by docker-compose to initialise the database containers.
# The API connects using the service name (e.g. "postgres"), not localhost.
POSTGRES_DB=traytic
POSTGRES_USER=traytic
POSTGRES_PASSWORD=traytic_secret

CLICKHOUSE_DB=traytic
CLICKHOUSE_USER=traytic
CLICKHOUSE_PASSWORD=traytic_secret


# ── API ───────────────────────────────────────────────────────────────────────

# Auth — REQUIRED: replace with any random 32+ character string
# Generate one: openssl rand -hex 32
BETTER_AUTH_SECRET=change_me_to_a_long_random_string_here
BETTER_AUTH_URL=http://localhost:3001

# Public URLs
APP_URL=http://localhost:3000
API_URL=http://localhost:3001


# ── Billing (optional — leave blank to skip) ──────────────────────────────────

# Paystack — for Africa (NG / GH / KE / ZA etc.)
PAYSTACK_SECRET_KEY=
PAYSTACK_PUBLIC_KEY=
PAYSTACK_WEBHOOK_SECRET=

# Polar — for global (USD / EUR)
POLAR_ACCESS_TOKEN=
POLAR_WEBHOOK_SECRET=
POLAR_ORGANIZATION_ID=


# ── Web (Next.js) ─────────────────────────────────────────────────────────────
# IMPORTANT: These are baked into the JS bundle at build time.
# For Coolify production: set these to your actual public domains before building.
# For local Docker: the defaults below work as-is.
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_APP_URL=http://localhost:3000
